# MySQL example
DB_DSN="mysql:host=127.0.0.1;port=3306;dbname=shopsignal;charset=utf8mb4"
DB_USER="shopsignal"
DB_PASSWORD="change-me"

# PostgreSQL also works through PDO, but StoreRepository's date and JSON
# aggregation expressions should then be changed to PostgreSQL equivalents.
